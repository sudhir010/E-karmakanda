{
  "name": "ekarmakanda",
  "description": "Preserving Ancient Hindu Rituals with Technology",
  "scripts": {
    "start": "http-server -p 8000 -c-1 --index index.php -a 127.0.0.1",
    "start:php": "php -S localhost:8000",
    "dev": "npx http-server -p 8000 -c-1 --index index.php -a 127.0.0.1"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
